flysystem:
    storages:
        default.storage:
            adapter: 'memory'
        documents.storage:
            adapter: 'memory'
        criteria.storage:
            adapter: 'memory'
        resolutions.storage:
            adapter: 'memory'
        judgments.storage:
            adapter: 'memory'
