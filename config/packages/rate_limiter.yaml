framework:
    rate_limiter:
        inbound_email:
            policy: sliding_window
            limit: 30
            interval: '1 minute'
        AGENT:
            policy: sliding_window
            limit: 60
            interval: '1 minute'
