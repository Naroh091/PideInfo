Analiza el documento adjunto: {{filename}}

# Contexto del expediente

{{inventory}}
{{orphan_context}}{{batch_context}}{{preassigned_hint}}
Recuerda la regla de desambiguación de los justificantes REG: si el expediente NO tiene todavía una solicitud y este PDF contiene el texto de lo solicitado, es `solicitud`; si YA la tiene, es `acuse_recibo` (o el tipo que corresponda por contenido). Determina también `origin` (quién firma/emite), `phase` y, si el organismo destinatario hay que registrarlo de cero, `publicBodyType`.

A continuación tienes el contenido del documento.
