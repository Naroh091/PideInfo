<nav class="glass border-b border-slate-200/50 sticky top-0 z-30">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
            <a href="{{ path('app_home') }}" class="flex items-center gap-2 text-primary-600 font-bold text-xl">
                <i data-lucide="search" class="w-6 h-6"></i>
                <span>PideInfo</span>
            </a>
            <div class="flex items-center gap-3">
                <a href="{{ path('app_resoluciones_index') }}"
                   class="text-sm font-medium transition-colors {{ active_page|default('') == 'resoluciones' ? 'text-primary-600' : 'text-slate-600 hover:text-primary-600' }}">
                    Resoluciones
                </a>
                <a href="{{ path('app_login') }}" class="text-sm text-slate-600 hover:text-primary-600 transition-colors font-medium">
                    Iniciar sesión
                </a>
                <a href="{{ path('app_register') }}" class="btn btn-primary btn-sm">
                    Crear cuenta
                </a>
            </div>
        </div>
    </div>
</nav>
