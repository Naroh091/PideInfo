<nav class="glass border-b border-slate-200/50 sticky top-0 z-30">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
            <div class="flex items-center gap-6">
                <a href="{{ path('app_home') }}" class="flex items-center gap-2 text-primary-600 font-bold text-xl">
                    <i data-lucide="search" class="w-6 h-6"></i>
                    <span>PideInfo</span>
                </a>
                <a href="{{ path('app_home') }}"
                   class="flex items-center gap-1.5 text-sm font-medium transition-colors {{ active_page|default('') == 'home' ? 'text-primary-600' : 'text-slate-600 hover:text-primary-600' }}">
                    <i data-lucide="home" class="w-4 h-4"></i>
                    Inicio
                </a>
                <a href="{{ path('app_resoluciones_index') }}"
                   class="flex items-center gap-1.5 text-sm font-medium transition-colors {{ active_page|default('') == 'resoluciones' ? 'text-primary-600' : 'text-slate-600 hover:text-primary-600' }}">
                    <i data-lucide="scale" class="w-4 h-4"></i>
                    Repositorio de resoluciones
                </a>
            </div>
            <div class="flex items-center gap-3">
                {% if app.user %}
                    <a href="{{ path('app_dashboard') }}" class="btn btn-primary btn-sm">
                        <i data-lucide="layout-dashboard" class="w-4 h-4 mr-1.5"></i>
                        Mi panel
                    </a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="text-sm text-slate-600 hover:text-primary-600 transition-colors font-medium">
                        Iniciar sesión
                    </a>
                    <a href="{{ path('app_register') }}" class="btn btn-primary btn-sm">
                        Crear cuenta
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
