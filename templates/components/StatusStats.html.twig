<div {{ attributes.defaults({class: 'status-stats', 'data-action': 'documents:processed@window->live#$render'}) }}>
    {% set stats = this.stats %}

    {% set statItems = [
        { count: stats.sent, label: 'Enviadas', icon: 'send', color: '#0ea5e9', variant: 'primary' },
        { count: stats.processing, label: 'En trámite', icon: 'loader', color: '#38bdf8', variant: 'info' },
        { count: stats.granted, label: 'Concedidas', icon: 'package', color: '#34d399', variant: 'success' },
        { count: stats.granted_completed, label: 'Completadas', icon: 'check-circle', color: '#059669', variant: 'complete' },
        { count: stats.delayed, label: 'Silencio', icon: 'clock', color: '#a78bfa', variant: 'accent' },
        { count: stats.appealed, label: 'Reclamadas', icon: 'scale', color: '#fbbf24', variant: 'warning' },
    ] %}

    {% if stats.total > 0 %}
    <div class="proportion-bar-container" style="animation: fade-slide-up 0.4s ease-out both">
        <div class="proportion-bar">
            {% for item in statItems %}
                {% if item.count > 0 %}
                    <div class="proportion-segment" style="width: {{ (item.count / stats.total * 100) }}%; background: {{ item.color }}"></div>
                {% endif %}
            {% endfor %}
        </div>
        <div class="flex flex-wrap gap-x-4 gap-y-1 mt-2">
            {% for item in statItems %}
                {% if item.count > 0 %}
                <div class="flex items-center gap-1.5 text-xs text-slate-500">
                    <span class="w-2 h-2 rounded-full flex-shrink-0" style="background: {{ item.color }}"></span>
                    <span>{{ item.label }} <strong class="text-slate-700">{{ item.count }}</strong></span>
                </div>
                {% endif %}
            {% endfor %}
        </div>
    </div>
    {% endif %}
</div>
